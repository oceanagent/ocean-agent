# ── 비밀 (절대 커밋 금지) ───────────────────────────────
# ── 사용자가 저장한 비밀 메모 (2026-08-10 push 차단 사고 방지) ──
pypi.txt
*recovery-code*
*recovery-codes*
*token*.txt
*비밀*
*백업코드*

.env
.env.*
!.env.example
*.key
*.pem

# ── 실계좌·실운영 데이터 (사생활/전략 노출) ──────────────
outputs/
predictions.json
state.json
bot.log
*.log
.mcp.json
.pacifica_changelog_last.txt
.pacifica_mcp_version.txt
known_mcp_tools.json

# ── 사용자 로컬 운영 설정 (개인별 값) ────────────────────
config.yaml
policy.yaml

# ── 내부 문서 (공개 부적합: 보안 상세·의사결정 로그) ─────
보안점검.md
코드검토.md
ROADMAP_STATUS.md
주간보고_*.md
폴더안내.md

# ── 내부 연구·측정 스크립트 (엣지 세부 비공개) ──────────
research/
testnet/
archive/

# ── 로컬 자동화 스크립트 (개인 워크플로) ────────────────
*.bat
*.vbs

# ── 빌드 산출물 ─────────────────────────────────────────
dist/
build/
*.egg-info/
__pycache__/
*.pyc
*.pyo

# ── 에이전트/에디터 설정 ────────────────────────────────
.claude/
.vercel/
.DS_Store
vercel_token.txt

# internal working documents, never publish
측정기록.md
의도적결정.md
목요일계획.md
개선루프.md
검토용_요약.md
데이터목록.md
문서지도.md
방향조사.md
보관/
사전등록_숏순위.md
작업규칙.md
작업현황.md
차트공부.md
# 08-20~21 에 만든 공부·검토 문서. 위와 같은 성격인데 목록에 없어서 추적되고
# 있었다. 안에 살아남은 엣지의 효과크기·t값·부호일치와 기각된 것들의 목록이
# 그대로 있고, 기각 목록은 같은 길을 가려는 사람의 몇 달을 아껴준다.
# A new study document appeared three times in two days and twice it was born
# outside this list, so match the shape instead of naming each one. Nothing we
# publish is named this way.
*공부*.md
조합공부.md
방향공부.md
신호공부.md
검토공부.md
신호공부_코인.md
신호공부_주식.md
신호공부_부록_코인.md
신호공부_부록_주식.md
# Dated by name, so match the pattern rather than the day: 개발자전달_0821.md
# was tracked again within a day of adding 0820 by hand.
개발자전달_*.md
개발자회신_*.md
수정계획_*.md
추천픽.md
홀드아웃_사전등록.md
nul

# review notes and simulation output stay local
전수검토_*.md
outputs/sim_report.txt
오픈베타작업*.md
# wheel/sdist extracted by release/preflight.py for its leak scan
audit/
# Mechanisms are assets and never ship (2026-08-31 decision). The pattern is
# deliberately wide: on 09-02 two copies (operator_rules.bak2 and
# operator_rules.new.py) sat outside a rule that matched the one filename
# exactly, carrying the same rules, one `git add -A` from being staged.
operator_rules*

# safebot is an operator tool: kept out of the wheel on 08-24 by moving it
# to the repo root, but it stayed tracked and so stayed in the public repo
# for every release since. No secrets in it (checked 09-07: no tokens, no
# addresses, no chat ids, reads no environment), so this only stops it
# going out from here on; the history keeps what was already pushed.
safebot.py

# 09-09 전수검토 4-4: 전략 메커니즘이 적힌 문서 셋이 안 걸려 있었다.
# git add -A 하면 공개 저장소로 나간다 (작업규칙 18조).
재측정_목록.md
개발자_배포검토_*.md
배포분류_*.md
